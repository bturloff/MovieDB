import React from "react";

const NavBar = () => {
  return (
    <div className={`navbar-fixed navbar`}>
      <nav className="blue-grey darken-4">
        <div className="nav-wrapper container">
          <span className="brand-logo">MovieDB</span>
        </div>
      </nav>
    </div>
  );
};

export default NavBar;
